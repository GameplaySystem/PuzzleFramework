# Puzzle Framework

This package contains the reusable, game-agnostic runtime code for the Puzzle Framework project.

It is intended to be consumed by separate prototype Unity projects through a local path dependency during MVP development.
